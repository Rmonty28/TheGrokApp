name: crimson_tapper
description: Black/red themed idle tapper game for iOS and Android.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.0

flutter:
  uses-material-design: true